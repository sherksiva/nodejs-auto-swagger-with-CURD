# nodejs-auto-swagger-with-CURD

Node js auto swagger generation and Signin signup services with JWT



Sample application with swagger CRUD


While run the application do below steps



1. npm install
2. node index.js

clean the code inside src/output/swagger-output.json ( inside the file delete the json code only / not a file )

